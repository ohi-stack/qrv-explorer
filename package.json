{
  "name": "qrv-explorer",
  "version": "1.0.0",
  "private": true,
  "description": "Public QR-V registry explorer for QRVID, hash, issuer, and record lifecycle lookups.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}